'use strict';

var WICEDSense = source("wiced-sense");

describe("Cylon.Drivers.BLE.WICEDSense", function() {
  var driver = new WICEDSense({
    device: { connection: 'connect' }
  });

  it("needs tests");
});
